{
  "name": "@pageblock/commerce-banner",
  "version": "1.0.0",
  "dependencies": {
    "@ijstech/components": "^0.1.10"
  },
  "scripts": {
    "build": "rm -rf dist && node esbuild.js && cp -f scconfig.json dist",
    "cpbuild": "npm run build && cp ./dist/commerce-banner.js ../mindlink/dist"
  },
  "devDependencies": {
    "esbuild": "^0.13.4"
  }
}
